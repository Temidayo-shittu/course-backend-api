/*
const EventEmitter= require('events');
//const url= 'http://mylogger.io/log'
class Logger extends EventEmitter{
    log(message){
        console.log(message);
        //Raise an event
    this.emit('messageLogged',{id:1, url:'http://'})
    //to emit is to raise a signal 
    }
}
 
module.exports= Logger;
*/